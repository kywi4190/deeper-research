<!--
Baseline slot for benchmark `cold-email-advice` (design §10 A/B).

Paste a plain Deep Research answer to the benchmark's question below this
comment, verbatim and unedited. Note: this benchmark's reference angle union
is still empty (the first run of the question seeds it) — the baseline
comparison only becomes scoreable once the union exists.

Until an answer is pasted (any non-comment content counts), the eval refuses
this file loudly instead of judging boilerplate.
-->
