<!--
Baseline slot for benchmark `vector-store-selection` (design §10 A/B).

Paste a plain Deep Research answer to the benchmark's question below this
comment, verbatim and unedited. `deeper eval <run> --against
vector-store-selection --compare-baseline` then scores this answer's angle
coverage against the same reference union, with the same judge, that scored
the run.

Until an answer is pasted (any non-comment content counts), the eval refuses
this file loudly instead of judging boilerplate.
-->
