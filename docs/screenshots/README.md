# Viewer screenshots

Placeholder for `deeper view` page captures, referenced from the README's
"The viewer" section. To populate: walk any mock run to Gate C
(`deeper new "..." --profile quick`, approve gates A/B), run
`deeper view <run>`, and capture:

- `run-list.png` — `/` (spend-vs-cap bars)
- `angle-map.png` — `/run/<run>/map` (prior + allocation bars)
- `option-cards.png` — `/run/<run>/options` (grid + critique badges)
- `rubric-gate-b.png` — `/run/<run>/rubric` (the Gate B form)
- `contenders-gate-c.png` — `/run/<run>/contenders` (scoreboards, sensitivity, Gate C form)
- `report.png` — `/run/<run>/report` (claim links into the appendix)
