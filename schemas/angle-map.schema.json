{
  "$defs": {
    "Angle": {
      "additionalProperties": false,
      "description": "A general solution area; a distinct region of the space.",
      "properties": {
        "id": {
          "pattern": "^[a-z0-9][a-z0-9-]*$",
          "title": "Id",
          "type": "string"
        },
        "name": {
          "minLength": 1,
          "title": "Name",
          "type": "string"
        },
        "definition": {
          "minLength": 1,
          "title": "Definition",
          "type": "string"
        },
        "distinctness_rationale": {
          "description": "Why this is a distinct region and not a variant of another angle.",
          "minLength": 1,
          "title": "Distinctness Rationale",
          "type": "string"
        },
        "example_options": {
          "description": "2-3 concrete options proving the angle is non-empty \u2014 existence proofs, never endorsements.",
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "minItems": 1,
          "title": "Example Options",
          "type": "array"
        },
        "relevance_prior": {
          "description": "0-1 estimated relevance; drives the S2 budget allocation.",
          "maximum": 1,
          "minimum": 0,
          "title": "Relevance Prior",
          "type": "number"
        },
        "prior_justification": {
          "description": "One paragraph grounded ONLY in the brief and destination model \u2014 never in preferences.",
          "minLength": 1,
          "title": "Prior Justification",
          "type": "string"
        },
        "contributing_heuristics": {
          "description": "Which cartographer framings produced or corroborated this angle.",
          "items": {
            "$ref": "#/$defs/Heuristic"
          },
          "minItems": 1,
          "title": "Contributing Heuristics",
          "type": "array"
        },
        "sub_angles": {
          "items": {
            "$ref": "#/$defs/SubAngle"
          },
          "title": "Sub Angles",
          "type": "array"
        },
        "notes": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Notes"
        }
      },
      "required": [
        "id",
        "name",
        "definition",
        "distinctness_rationale",
        "example_options",
        "relevance_prior",
        "prior_justification",
        "contributing_heuristics"
      ],
      "title": "Angle",
      "type": "object"
    },
    "DedupEntry": {
      "additionalProperties": false,
      "description": "One raw cartographer angle and where the merger folded it. This mapping is\nthe input to the saturation rule's marginal-novelty computation.",
      "properties": {
        "heuristic": {
          "$ref": "#/$defs/Heuristic"
        },
        "raw_name": {
          "minLength": 1,
          "title": "Raw Name",
          "type": "string"
        },
        "merged_into": {
          "pattern": "^[a-z0-9][a-z0-9-]*$",
          "title": "Merged Into",
          "type": "string"
        }
      },
      "required": [
        "heuristic",
        "raw_name",
        "merged_into"
      ],
      "title": "DedupEntry",
      "type": "object"
    },
    "Heuristic": {
      "description": "The cartographer framing personas \u2014 ensemble diversity is the breadth\nmechanism (design P3, \u00a75/S1). HUMAN is not a cartographer: it marks angles\nthe human added at Gate A, so provenance survives into the map.",
      "enum": [
        "first-principles",
        "analogist",
        "contrarian",
        "practitioner",
        "taxonomist",
        "horizon-scanner",
        "human"
      ],
      "title": "Heuristic",
      "type": "string"
    },
    "SubAngle": {
      "additionalProperties": false,
      "description": "Second (and final) taxonomy level \u2014 'angle \u2192 sub-angle where warranted'.",
      "properties": {
        "name": {
          "minLength": 1,
          "title": "Name",
          "type": "string"
        },
        "definition": {
          "minLength": 1,
          "title": "Definition",
          "type": "string"
        }
      },
      "required": [
        "name",
        "definition"
      ],
      "title": "SubAngle",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "The merged angle map \u2014 angles/map.yaml, reviewed at Gate A.",
  "properties": {
    "angles": {
      "items": {
        "$ref": "#/$defs/Angle"
      },
      "minItems": 1,
      "title": "Angles",
      "type": "array"
    },
    "dedup_map": {
      "items": {
        "$ref": "#/$defs/DedupEntry"
      },
      "title": "Dedup Map",
      "type": "array"
    },
    "notes": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Notes"
    }
  },
  "required": [
    "angles"
  ],
  "title": "AngleMap",
  "type": "object"
}
